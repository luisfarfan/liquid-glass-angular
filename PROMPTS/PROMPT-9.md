Now generate a task breakdown for AI coding agents (Cursor, Claude, Antigravity).

For each task include:

- task title
- objective
- inputs (spec sections required)
- outputs (expected files/components)
- dependencies
- step-by-step instructions
- definition of done

The tasks must:
- follow the implementation phases
- be executable independently
- be precise and unambiguous

Goal:
Allow an AI agent to build the entire system step by step without guessing.