{
  "name": "glassng",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/glassng/src",
  "prefix": "lib",
  "projectType": "library",
  "release": {
    "version": {
      "manifestRootsToUpdate": [
        "libs/glassng",
        "{workspaceRoot}/dist/libs/glassng"
      ],
      "currentVersionResolver": "git-tag",
      "fallbackCurrentVersionResolver": "disk"
    }
  },
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/angular:package",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "options": {
        "project": "libs/glassng/ng-package.json",
        "tsConfig": "libs/glassng/tsconfig.lib.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "libs/glassng/tsconfig.lib.prod.json"
        },
        "development": {}
      },
      "defaultConfiguration": "production"
    },
    "nx-release-publish": {
      "dependsOn": ["build"],
      "executor": "nx:run-commands",
      "options": {
        "command": "npm publish dist/libs/glassng --access public --tag {args.tag} --verbose"
      }
    },
    "test": {
      "executor": "@nx/angular:unit-test",
      "options": {
        "reportsDirectory": "../../coverage/libs/glassng"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
